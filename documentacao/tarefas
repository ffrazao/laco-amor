LACO DE AMOR

fazer o cadastro de usuarios
	fazer o form do usuario
fazer a atualizaçao do token oauth (usar a opcao refresh token do oauth)
implementar a opção para filtrar por array de ids com opção de ids que devem ser inclusos e excluídos => (com utilização do caractere ! antes do valor do parâmetro).

padronizar respostas do servico com ResponseEntity



ok => colocar meu nome no rodape
ok => colocar este email para contato
ok => colocar copyright 2020
ok => ativar o envio de email
ok => colocar foto de perfil do usuario
ok => configurar os cruds
ok => unificar o metodo maximoLinhas
ok (conceito errado) => primeira coisa só pode haver um produto modelo / produto vigente no evento inteiro (tratar a gravação dos produtos antes de gravar o evento)

GERADOR DE CODIGO

ok => A estrategia correta de chave primaria é strategy = GenerationType.IDENTITY e não strategy = GenerationType.AUTO
ajustar o metodo novo no gerador de codigo tanto em rest como em bo ele é abstrato em bo tb